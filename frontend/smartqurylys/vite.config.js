    // vite.config.js
    import { defineConfig } from 'vite';

    export default defineConfig({
      server: {
        allowedHosts: true, // Разрешает все хосты :cite[1]:cite[5]
    host: true
        // Other server options...
      },
      // Other Vite configuration options...
    });
