spring:
  application:
    name: smartqurylys-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/smartqurylys_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    # show-sql: true
    # properties:
    #   hibernate:
    #     format_sql: true

  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:5432/smartqurylys_db
    user: postgres
    password: postgres
    locations: classpath:db/migration
    repair-on-migrate: true
    
  mail:
    host: smtp.gmail.com
    port: 587
    username: gkey2014@gmail.com
    password: aayhpozlaogtiimq
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    transport:
      protocol: smtp
    debug: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB

server:
  port: 8080
  tomcat:
    connection-timeout: 60000

jwt:
  secret: mySuperSecretKeyThatIsAtLeast32CharsLong
  expiration: 2592000000

twilio:
  account:
    sid: "{TWILIO_ACCOUNT_SID}"
  auth:
    token: "{TWILIO_AUTH_TOKEN}"
  phone:
    number: "+14177644038"

